# how to test with forge client?
first, don't run with ``./gradlew forge:runClient``.
if you're running at first time, run ``./gradlew forge:setupAris``.
then you can run ``./gradlew forge:arisClient`` for test!